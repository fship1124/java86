package annotation.exam03;

public class Test {
	// default로 설정된 항목은 값을 선택적으로 넣는다.
//	@FullValue(name="a", type="b")   // 속성이 여러개일경우 전부 기입해줘야함
//	@FullValue(name="a")			 // type 속성을 주지않아도 오류가 나지 않음. default 설정해놓은 상태 
//	@FullValue(type="a")			 // error 발생함
	public void call() {
		
	}

}
